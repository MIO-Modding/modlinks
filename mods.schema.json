{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/ShackledMars261/mio-modlinks/blob/main/mods.schema.json",
  "title": "Modlinks",
  "description": "A list of mods for MIO: Memories in Orbit.",
  "type": "array",
  "items": {
    "type":"object",
    "required": ["id", "titleSlug", "displayTitle", "authorSlug", "displayAuthor", "description", "versions", "dependencies"],
    "additionalProperties": false,
    "properties": {
        "id": {
            "type": "string",
            "description": "An identifier for your mod."
        },
        "titleSlug": {
            "type": "string",
            "description": "The repository slug from the URL of your github repository. Used for linking to the repository."
        },
        "displayTitle": {
            "type": "string",
            "description": "How you want your mod's name to be displayed."
        },
        "authorSlug": {
            "type": "string",
            "description": "The username slug from the URL of your github account. Used for linking to the repository and your account."
        },
        "displayAuthor": {
            "type": "string",
            "description": "How you want your username to be displayed."
        },
        "description": {
            "type": "string",
            "description": "A description of your mod."
        },
        "versions": {
            "type": "object",
            "description": "A list of downloads for your mod, by semantic version.",
            "additionalProperties": false,
            "minProperties": 1,
            "patternProperties": {
                "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)$": {
                    "type": "string",
                    "description": "URL to the .zip download for your mod."
                }
            }
        },
        "dependencies": {
            "type": "array",
            "description": "A list of the identifiers of mods your mod depends on.",
            "items": {
                "type": "string"
            }
        }
    }
  },
  "minItems": 1
}